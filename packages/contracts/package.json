{
  "name": "fanio-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Fanio crowdfunding platform",
  "scripts": {
    "deploy:local": "forge script script/DeployFundingManager.s.sol --rpc-url anvil --broadcast && npm run update-config && npm run update-frontend",
    "deploy:base-sepolia": "forge script script/DeployFundingManager.s.sol --rpc-url base_sepolia --broadcast && npm run update-config && npm run update-frontend",
    "update-frontend": "node scripts/update-frontend-config.js",
    "update-config": "node scripts/update-config.js",
    "test": "forge test",
    "test:verbose": "forge test -v",
    "compile": "forge build",
    "clean": "forge clean"
  },
  "keywords": [
    "solidity",
    "ethereum",
    "crowdfunding",
    "foundry"
  ],
  "author": "Fanio Team",
  "license": "MIT",
  "devDependencies": {
    "foundry": "^0.2.0"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
